Print "============================"
Print "==Detector Numero Positivo=="
Print "============================"

INPUT "Ingrese un numero --->  ", numero

IF numero > 0 THEN
    PRINT "El numero es positivo"

ELSE IF numero < 0 THEN
    PRINT "El numero es negativo"
ELSE
    PRINT "El numero es cero"
END IF